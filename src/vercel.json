{
  "version": 2,
  "builds": [
    { "src": "api/think.ts", "use": "@vercel/node" },
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/api/think", "dest": "/api/think.ts" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
